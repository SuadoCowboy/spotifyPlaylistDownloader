If you already have pytube installed, remove the pytube requirement in
requirements.txt. It installs pytube from another github repo because he
did a fix for a bug the most recent version of pytube(15.0.0) has.